# devops/ holds every per-deployment file: rendered Compose files and real Traefik dynamic
# config (devops/traefik/bench-00.yml, etc.). Only the example.* templates are tracked; everything
# else is local to the checkout, same pattern as .env below.
devops/**
!devops/**/
!devops/**/example.*

.env
