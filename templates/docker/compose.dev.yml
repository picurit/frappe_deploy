x-customizable-image: &customizable_image
  restart: no

x-frappe-defaults: &frappe_defaults
  <<: *customizable_image

services:
  configurator:
    <<: *frappe_defaults
  frappe:
    <<: *frappe_defaults
  db:
    healthcheck:
      start_period: 60s
      retries: 10
